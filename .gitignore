# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# runtime data (contact messages, leaderboard)
/server/data/*.json

# misc
.DS_Store
*.pem
*.tsbuildinfo

# env
.env*

# raw hero-model kit (39MB, never needed at runtime — public/models/f1-car.glb
# and public/models/tex/* are the already-built output the app actually serves)
/assets-src/
